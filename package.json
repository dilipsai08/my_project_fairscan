{
  "name": "fairscan-root",
  "version": "1.0.0",
  "private": true,
  "description": "Root package for Fairscan",
  "scripts": {
    "install-all": "cd Frontend && npm install && cd ../Backend && npm install"
  }
}
